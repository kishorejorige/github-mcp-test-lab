[project]
name = "github-mcp-test-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pytest>=9.1.1",
]

[dependency-groups]
dev = [
    "ruff>=0.16.1",
]
